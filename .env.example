DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DB?sslmode=require"

# used later for hashing tokens (DO NOT reuse your DB password)
AUTH_TOKEN_PEPPER="replace-with-long-random-secret"

# used later for session cookie settings
SESSION_COOKIE_NAME="offerlog_session"

RESEND_API_KEY="re_..."
APP_BASE_URL="http://localhost:3000"

RESEND_FROM="OfferLog <onboarding@yourdomain.com>"
NEXT_PUBLIC_APP_URL = "http://localhost:3000"